{
  "name": "play-express",
  "version": "1.0.0",
  "description": "express native stuff maan experiments & tests",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server",
    "static": "node --watch server",
    "json": "node --watch --env-file=.env serverJSON",
    "es6": "node --watch --env-file=.env serverES6"
  },
  "keywords": [
    "node",
    "express",
    "js"
  ],
  "author": "IMxSuhasTheBoy",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "express": "^4.19.2"
  }
}
